<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="me.realimpact.telecom.calculation.infrastructure.adapter.mybatis.CalculationResultMapper">

    <!-- 평면화된 계산 결과를 배치로 삽입 -->
    <insert id="batchInsertCalculationResults" parameterType="java.util.List">
        INSERT INTO calculation_result (
            contract_id,
            billing_start_date,
            billing_end_date,
            product_offering_id,
            charge_item_id,
            revenue_item_id,
            effective_start_date,
            effective_end_date,
            suspension_type,
            fee,
            balance
        ) VALUES
        <foreach collection="items" item="item" separator=",">
        (
            #{item.contractId},
            #{item.billingStartDate},
            #{item.billingEndDate},
            #{item.productOfferingId},
            #{item.chargeItemId},
            #{item.revenueItemId},
            #{item.effectiveStartDate},
            #{item.effectiveEndDate},
            #{item.suspensionType},
            #{item.fee},
            #{item.balance}
        )
        </foreach>
    </insert>

    <!-- 모든 계산 결과를 삭제 (테스트 및 전체 초기화 용도) -->
    <delete id="deleteAllCalculationResults">
        DELETE FROM calculation_result
    </delete>

</mapper>